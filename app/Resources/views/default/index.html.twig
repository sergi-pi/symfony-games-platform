{% extends 'base.html.twig' %}



{% block body %}
    <div id="sidebar-wrapper">
        <nav id="spy">
            <ul class="sidebar-nav nav">
                {% for category in categories %}
                <li class="sidebar-brand">
                    <a href="#"><span class="fa fa-home solo">{{ category.name }}</span></a>
                </li>
                {% endfor %}
            </ul>
        </nav>
    </div>

{% endblock %}
