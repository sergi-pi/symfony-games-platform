{% extends 'base.html.twig' %}

{% block body %}
    <div class="joc text-center" data-game="{{ chosenGame.id }}" data-user="{{ app.user.id }}">
        <h1 id="score"></h1>
        <canvas id="tetris" width="240" height="400"></canvas>
    </div>
    <script src="{{ chosenGame.scriptLocation }}"></script>
{% endblock %}